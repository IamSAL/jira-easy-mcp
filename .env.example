# Jira MCP Server Configuration (Basic Authentication)

# Your Jira instance URL (required)
JIRA_BASE_URL=https://your-jira-instance.com

# Jira username (required)
JIRA_USERNAME=your-username

# Jira password (required)
# For Jira Cloud: use an API token instead of password
#   - Generate at: https://id.atlassian.com/manage-profile/security/api-tokens
# For Jira Server: use your regular password
JIRA_PASSWORD=your-password-or-api-token

# Project filter (optional)
# Comma-separated list of project keys to limit access
# If not set, all accessible projects will be available
# JIRA_PROJECTS_FILTER=ABC,DEF,XYZ

# Response format (optional)
# JSON: Standard JSON format (default)
# TOON: Text Object-Oriented Notation - more human-readable, compact format
# JIRA_RESPONSE_FORMAT=JSON

# =============================================================================
# Advanced Configuration (all optional)
# =============================================================================

# Request timeout in milliseconds (default: 30000 = 30 seconds)
# JIRA_TIMEOUT=30000

# Number of retries for failed requests (default: 3)
# Retries on: 429 (rate limit), 502, 503, 504, network errors, timeouts
# JIRA_RETRY_COUNT=3

# Base delay between retries in milliseconds (default: 1000)
# Uses exponential backoff: delay * 2^attempt + jitter
# JIRA_RETRY_DELAY=1000

# SSL certificate verification (default: true)
# Set to false to skip SSL verification for self-signed certificates
# WARNING: Only use for development/testing with self-signed certs
# JIRA_SSL_VERIFY=true

# Log level for MCP output (default: INFO)
# DEBUG: Shows API requests/responses, cache hits, detailed info
# INFO: Shows tool calls with input/output summaries
# WARN: Shows warnings and errors only
# ERROR: Shows errors only
# JIRA_LOG_LEVEL=INFO

# Cache TTL in seconds for static data (default: 300 = 5 minutes)
# Caches: fields, link types, and other rarely-changing data
# JIRA_CACHE_TTL=300
